<div class="row">
  <div class="col-md-6">
    <h1>Welcome to Matchmaking</h1>
    <h2>
      Matchmaking is the platform insprired by the games you play.
      you can search our database of games and see what the most played games are right now. Add the games you own to your personal library and then join the list of users looking for groups in that game. 
     
    </h2>
  </div>
  <div class="col-md-6";>
<h2>Log in</h2>
 
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :password %><br />
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>
<br>
  <div class="actions">
    <%= f.submit "Log in" %>
  </div>
  </div>

  <% end %>


<%= render "devise/shared/links" %>
